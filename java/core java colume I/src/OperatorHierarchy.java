/**
 * Created by sb on 2017/9/11.
 */
public class OperatorHierarchy {
    public static void main(String[] args) {
        /// associativity

        // a && b || c
        // means
        // (a && b) || c

        // a += b += c
        // means
        // a += (b += c)
    }
}
